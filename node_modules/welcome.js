module.exports = client => {
    const channelId = "channel_id";
    const rulesChannel = "channel_id"
    client.on("guildMemberAdd", (member) => {
      console.log(member);

      const message = `Welcome <@${member.id}> to our server! Be sure to check out our ${member.guild.channels.cache.get(rulesChannel).toString()}`;

      const channel = member.guild.channels.cache.get(channel_Id);
      channel.send(message);
    });
};